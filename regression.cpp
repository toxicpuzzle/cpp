// Create a linear regression fitter that can predict
class Regression{
    public:
        Regression(){
            // Initialise key variables
        };

        void fit(int training[]){
            // Given some data represented as a multidimensional array
            // Trains the regression model onto the training data
            // Start with loss function = MSE, but allow custom loss functions later
        }

        void predict(int sample[]){
            // Predicts what class the sample will be using the fit function
        }
};
    